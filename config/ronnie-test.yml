server:
  port: 8762
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Rainstorm-zcw/spring-cloud-config.git
          default-label: master
          search-paths: config1
  profiles:
    active: test